{
  "name": "orbitwork-workspace",
  "version": "1.0.0",
  "description": "Orbitjob AI Task Marketplace Workspace",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "opencode": "node scripts/opencode.js"
  },
  "devDependencies": {
    "opencode-ai": "latest"
  },
  "dependencies": {
    "vercel": "^53.3.2"
  }
}
